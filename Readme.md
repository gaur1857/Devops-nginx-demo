# This project is Demo project

# intro


# this project is benifical for us to install nginx.


# this is not the big project.


# Steps

sudo apt update
sudo apt install nginx -y

sudo systemctl start nginx
sudo systemctl enable nginx
